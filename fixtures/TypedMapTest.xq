
declare 
function local:f1( $var1 as map(xs:integer, xs:string) ) {
    'example 1: The MapTest map(X, Y) -
     matches any map where the type of every key is an instance of X 
     and the type of every value is an instance of Y. 
     '
};


()